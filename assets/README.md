`logo.webp` / `logo.png` are the header logo, resized to 465x124 (2x the display
height) from `Clutch Clicks Site Logo.png`, which is kept here as the master.
The page serves the WebP and falls back to the PNG.

Optional: crisp testimonial poster frames, referenced via data-poster.
